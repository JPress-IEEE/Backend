import * as bookmarkController from '../controllers/bookmark.controller';
import { Router } from 'express';

/*
const bookmarkRouter = Router();

bookmarkRouter.get('/', bookmarkController.getBookmarks);
bookmarkRouter.get('/:id', bookmarkController.getBookmark);
bookmarkRouter.post('/', bookmarkController.createBookmark);
bookmarkRouter.put('/:id', bookmarkController.updateBookmark);
bookmarkRouter.delete('/:id', bookmarkController.deleteBookmark);

export default bookmarkRouter;
*/