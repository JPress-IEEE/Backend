import Bookmark,{IBookmark}  from "../models/bookmark.model";
import { BookmarkSchema } from "../schemas/bookmark.schema";
/*

export const createBookmark = async (bookmark: IBookmark): Promise<IBookmark> => {
  
};

export const getBookmarkById = async (bookmarkId: string): Promise<IBookmark | null> => {
   
};

export const getBookmarkByUserId = async (userId: string): Promise<IBookmark | null> => {
   
};

export const getBookmarkByOfferId = async (offerId: string): Promise<IBookmark | null> => {
   
};

export const getBookmarks = async (): Promise<IBookmark[]> => {
   
};

export const updateBookmark = async (bookmarkId: string, bookmark: IBookmark): Promise<IBookmark | null> => {
   
};

export const deleteBookmark = async (bookmarkId: string): Promise<IBookmark | null> => {
   
};

export const deleteBookmarkByUserId = async (userId: string): Promise<IBookmark | null> => {
   
};

export const deleteBookmarkByOfferId = async (offerId: string): Promise<IBookmark | null> => {
   
};

*/