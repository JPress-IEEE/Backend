import Recommendation, {IRecommendation} from "../models/recommendation.model";
import { RecommendationSchema } from "../schemas/recommendation.schema";

/*
export const createRecommendation = async (recommendation: IRecommendation): Promise<IRecommendation> => {
    
};

export const getRecommendationById = async (recommendationId: string): Promise<IRecommendation | null> => {
     
};

export const getRecommendationByUserId = async (userId: string): Promise<IRecommendation | null> => {
     
};

export const getRecommendations = async (): Promise<IRecommendation[]> => {
     
};

export const updateRecommendation = async (recommendationId: string, recommendation: IRecommendation): Promise<IRecommendation | null> => {
     
};

export const deleteRecommendation = async (recommendationId: string): Promise<IRecommendation | null> => {
     
};

export const deleteRecommendationByUserId = async (userId: string): Promise<IRecommendation | null> => {
     
};

*/