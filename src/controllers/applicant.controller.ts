import { Request, Response } from 'express';
import * as applicantServices from '../services/applicant.services';
import { IApplicant } from '../models/applicant.model';
import {ApplicantSchema} from '../schemas/applicant.schema';

/*

export const createApplicant = async (req: Request, res: Response): Promise<void> => {
};

export const getApplicantById = async (req: Request, res: Response): Promise<void> => { 
};

export const getApplicantByEmail = async (req: Request, res: Response): Promise<void> => {  
};

export const getApplicants = async (req: Request, res: Response): Promise<void> => { 
};

export const updateApplicant = async (req: Request, res: Response): Promise<void> => {   
};

export const deleteApplicant = async (req: Request, res: Response): Promise<void> => {   
};
*/
